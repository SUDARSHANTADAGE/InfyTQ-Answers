ELECT x.SID, x.SNAME FROM  
(SELECT s3.SID, S3.SNAME FROM Salesman S3) x
LEFT OUTER JOIN
(SELECT S1.SID, S1.SNAME FROM Salesman S1 INNER JOIN Sale S2 ON S1.SID=S2.SID GROUP BY S1.SID,S1.SNAME) y
ON 
y.SID = x.SID
WHERE y.SID IS NULL
